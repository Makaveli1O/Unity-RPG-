using UnityEngine;

/// <summary>
/// Handles chunk's  history and state handling
/// </summary>
public class ChunkHandler : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
